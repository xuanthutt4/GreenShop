<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarStyle">@style/myActionBar</item>
        <item name="drawerArrowStyle">@style/myDrawerArrowToggle</item>
    </style>
    <style name="myDrawerArrowToggle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="android:layout_marginTop">20dp</item>
    </style>
    <style name="myActionBar" parent="Widget.AppCompat.Light.ActionBar">
        <item name="android:titleTextStyle">@style/myTitleBar</item>
        <item name="titleTextStyle">@style/myTitleBar</item>
        <item name="android:background">@color/colorBlue</item>
        <item name="background">@color/colorBlue</item>
        <item name="height">70dp</item>
        <item name="android:height">70dp</item>
        <item name="android:layout_gravity">center</item>
    </style>
    <style name="myTitleBar" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:paddingLeft">5dp</item>
    </style>
</resources>
